image: node:latest
before_script:
  - yarn install
cache:
  paths:
    - node_modules/
test:
  stage: test
  script:
    - yarn run test
pages:
  stage: deploy
  script:
    - yarn run build
    - rm -rf public && mkdir public
    - mv dist/* public
  artifacts:
    paths:
      - public
  only:
    - master
